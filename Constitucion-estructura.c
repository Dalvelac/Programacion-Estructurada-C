// Enunciado:     Defina una estructura Estudiante que contenga nombre (una cadena de caracteres), edad (un entero) y promedio (un flotante).

#include <stdio.h>

struct Estudiante {
    char nombre[50];
    int edad;
    float promedio;
};
